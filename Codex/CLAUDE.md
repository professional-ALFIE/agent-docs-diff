# Codex — https://learn.chatgpt.com/docs 의 문서를 메뉴 계층대로 받은 폴더

문서를 찾을 때는 이 폴더의 `llms-local.txt` 를 연다. 들여쓰기 트리가 공식 사이트 메뉴 계층 그대로고 문서마다 공식 llms.txt 의 설명이 붙어 있다.
파일의 경로는 트리에서 조상 폴더들 + 파일명이고, 각 파일 맨 위 `> 원본:` 줄에 공식 URL 이 적혀 있다.
이 폴더의 내용은 `scripts/sync.ts` 가 생성한다 — 손으로 고치면 다음 회차에 덮인다.
